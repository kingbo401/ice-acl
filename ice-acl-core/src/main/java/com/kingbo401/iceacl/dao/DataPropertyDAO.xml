<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="om.kingbo401.iceacl.dao.DataPropertyDAO">
    <select id="getByIds" resultType="DataPropertyDO">
        select * from ice_data_property where status != 0 and id in
        <foreach collection="ids"  item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="getByCodes" resultType="DataPropertyDO">
        select * from ice_data_property where status != 0 and app_key = #{appKey} and
        code in
        <foreach collection="propertyCodes"  item="item" open="(" separator="," close=")">
             #{item}
        </foreach>
    </select>
    <select id="getByCode" resultType="DataPropertyDO">
        select * from ice_data_property where status != 0 and app_key = #{appKey} and code = #{code}
    </select>

	<sql id="queryPropertySubSql">
		where app_key = #{appKey} and status != 0
        <if test = "code != null and code != ''">
            and code = #{code}
        </if>
        <if test = "name != null and name != ''">
           and name = #{name}
        </if>
        <if test = "status != null and status != ''">
            and status = #{status}
        </if>
	</sql>
	
    <select id="listDataProperty" resultType="DataPropertyDO" parameterType="DataPropertyQueryParam" >
        select * from ice_data_property
        <include refid="queryPropertySubSql" />
        <if test = "orderField != null and orderField != ''">
		    order by ${orderField} ${orderType}
		</if>
    </select>

    <!-- 分页查询属性 -->
    <select id="pageDataProperty" resultType="DataPropertyDO" parameterType="DataPropertyQueryParam" >
        select * from ice_data_property
        <include refid="queryPropertySubSql" />
        <if test = "orderField != null and orderField != ''">
		    order by ${orderField} ${orderType}
		</if>
            limit #{offset},#{pageSize}
    </select>

    <select id="countDataProperty" resultType="java.lang.Integer" parameterType="DataPropertyQueryParam" >
        select count(1) from ice_data_property
        <include refid="queryPropertySubSql" />
    </select>

    <insert id="create" useGeneratedKeys="true" keyProperty="id" parameterType="DataPropertyDO">
        insert into ice_data_property (app_key, code,name, description, status,create_time, update_time)
        values (#{appKey}, #{code}, #{name}, #{description}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="DataPropertyDO" >
        update ice_data_property
        <set >
            <if test="name != null" >
                name = #{name},
            </if>
            <if test="description != null" >
                description = #{description},
            </if>
            <if test = "status != null">
                status = #{status},
            </if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>
</mapper>