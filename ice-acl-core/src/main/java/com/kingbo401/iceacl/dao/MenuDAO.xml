<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.kingbo401.iceacl.dao.MenuDAO">
    <select id="listMenu" resultType="MenuDO">
		select * from ice_menu where group_id=#{appKey} and status != 0 
		<if test="status != null">
          	status = #{status},
      	</if>
		order by menu_order asc
	</select>
	
    <insert id="createMenu" parameterType="MenuDO" useGeneratedKeys="true" keyProperty="id">
          insert into ice_menu(app_key,menu_name,menu_order,menu_pid,menu_url,menu_icon,menu_type,update_time,create_time,status)
          values(#{appKey},#{menuName},#{menuOrder},#{menuPid},#{menuUrl},#{menuIcon},#{menuType},#{updateTime},#{createTime},#{status});
    </insert>

    <update id="updateMenu" parameterType="MenuDO">
          update ice_menu
          <set>
              <if test="menuName != null and menuName != ''">
                  menu_name = #{menuName},
              </if>
              <if test="menuOrder != null">
                  menu_order = #{menuOrder},
              </if>
              <if test="status != null">
                  status = #{status},
              </if>
              <if test="menuUrl != null and menuUrl != ''">
                  menu_url = #{menuUrl},
              </if>
              <if test="menuPid != null and menuPid != ''">
                  menu_pid = #{menuPid},
              </if>
              <if test="menuType != null and menuType != ''">
                  menu_type = #{menuType},
              </if>
              <if test="menuIcon != null and menuIcon != ''">
                  menu_icon = #{menuIcon},
              </if>
              update_time = #{updateTime}
          </set>
           where id = #{id}
    </update>

    <!--根据id查询-->
    <select id="getMenuById" resultType="MenuDO">
        select * from ice_menu where id = #{id} and status != 0
    </select>
    
    <select id="getMenuByIds" resultType="MenuDO">
        select * from ice_menu where id in
        <foreach collection="ids" open="(" close=")" item="id" separator=",">
            #{id}
        </foreach>
        and status != 0
    </select>

    <!--根据pid查询-->
    <select id="listMenuByPid" resultType="MenuDO">
        select * from ice_menu where menu_pid = #{menuPid} and status != 0 order by menu_order asc
    </select>

    <!--查询用户拥有权限的菜单-->
    <select id="listUserMenu" resultType="MenuDO">
         select t4.*
         from ice_user_role_ref t1, ice_role t2, ice_role_menu_ref t3, ice_menu t4
         where t1.user_id=#{userId} and t1.tenant=#{tenant} and t4.app_key=#{appKey}
         and t1.role_id=t2.id and t1.role_id=t3.role_id and t3.menu_id=t4.id
         and t1.status=1 and t2.status=1 and t3.status=1 and t4.status=1
         order by menu_order asc
    </select>
</mapper>